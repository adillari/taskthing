<%= modal(title: "Delete \"#{@task.title.strip}\"?") do %>
  <%= button_to("Delete", task_path(@task, board_id: @task.board.id), class: "button-filled float-right bg-violet-700", autofocus: true, method: :delete, data: { turbo_frame: dom_id(@task.lane) }) %>
<% end %>
