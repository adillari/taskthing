<%= modal(title: t("modals.titles.delete_resource?", resource: @task.title.strip)) do %>
  <%= button_to(t("modals.confirms.delete!"), task_path(@task, board_id: @task.board.id), class: "button-filled float-right bg-violet-700", autofocus: true, method: :delete, data: { turbo_frame: dom_id(@task.lane) }) %>
<% end %>
