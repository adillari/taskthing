<%= render ModalComponent.new(title: "Delete \"#{@board.title}\"?") do %>
  <%= button_to "Delete", @board, class: "button-filled float-right bg-violet-700", autofocus: true, method: :delete, data: { turbo_frame: :_top } %>
<% end %>
