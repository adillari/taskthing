<%= modal(title: t("modals.titles.delete_resource?", resource: @board.title)) do %>
  <%= button_to t("modals.confirms.delete!"), @board, class: "button-filled float-right bg-violet-700", autofocus: true, method: :delete, data: { turbo_frame: :_top } %>
<% end %>
